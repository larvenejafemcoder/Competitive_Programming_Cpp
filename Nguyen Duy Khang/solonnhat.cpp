#include <bits/stdc++.h>
using namespace std;
int a,b;
int main(){
    cout<<"Nhap hai so tim so lon nhat ";
    cin>>a>>b;
    if (a > b) cout<<"So lon nhat la "<<a;
    else if (a < b) cout<<"So lon nhat la "<<b;
    else if (a = b) cout<<"Hai so bang nhau";
    return 0;
}

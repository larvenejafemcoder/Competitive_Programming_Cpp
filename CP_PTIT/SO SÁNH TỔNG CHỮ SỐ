#include <stdio.h>
int main()
{
	int i,t;
	int a=0;
	int b=0;
	scanf("%d %d",&i,&t);
	int m=i,n=t;
	while(m!=0)
	{
		a+=m%10;
		m/=10;
	}
	while(n!=0)
	{
		b+=n%10;
		n/=10;
	}
	if(a<=b)
	printf("%d %d",i,t);
	else
	printf("%d %d",t,i);
}

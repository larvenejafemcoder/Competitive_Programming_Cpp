#include<stdio.h>
int main()
{
	int a;
	scanf("%d",&a);
	while(a--)
	{
		long long n;
		scanf("%lld",&n);
		long long S=0;
		long long m=n;
		while(m>0)
		{
			S=S*10+m%10;
			m/=10;
		}
		if(S==n)
		printf("YES\n");
		else
		printf("NO\n");
	}
}

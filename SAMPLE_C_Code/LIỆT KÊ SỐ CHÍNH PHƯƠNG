#include <stdio.h>
#include <math.h>

int main() 
{
	int m,n;
	scanf("%d %d", &m, &n);
	int i;
	int a=ceil(sqrt(m));
	int b=sqrt(n);
	printf("%d\n",b-a+1);
	for(i=a;i<=b;i++)
	{
		printf("%lld\n",(long long)i*i);
	}
	return 0;

}
